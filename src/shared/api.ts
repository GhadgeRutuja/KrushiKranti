/**
 * Re-export the canonical Axios instance from services/api.ts.
 * This file exists for backward compatibility — all modules should
 * prefer importing from '../../services/api' directly.
 */
export { default } from '../services/api';
